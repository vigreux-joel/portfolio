---
import {Header} from "@components/Header"

interface Props {
  showFab?: boolean;
}

const { showFab = true } = Astro.props;
---
<header>
  <Header client:only="react" showFab={showFab}/>
</header>
