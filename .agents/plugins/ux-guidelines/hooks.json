{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node ${PLUGIN_ROOT}/tools/hook-drift.mjs ${TOOL_FILE_PATH}",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
